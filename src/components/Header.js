import React from 'react';

const Header = () => {
    return (
        <header>
            <h2>Calendar App</h2>
            <a href='#' className='log-button button'>
                Logout
            </a>
        </header>
    )
};

export default Header;